package projektoo;

import java.awt.Color;

public class Sky extends Rectangle{
	public Sky(Model model) {
		super(0, 0, model.getResX(), model.getResY(), 0, 0, 0, 0, model, Color.BLUE, 0, 0, 0);
	}
}
